using AdaptiveScrolls;

public class TestScroll : AdaptiveScroll<TestScrollItem>
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
