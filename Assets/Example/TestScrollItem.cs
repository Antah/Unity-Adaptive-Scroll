using AdaptiveScrolls;

public class TestScrollItem : AdaptiveScrollItem
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
